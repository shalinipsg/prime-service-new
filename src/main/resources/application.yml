spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/primes
    username: postgres
    password: harshi23A

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

server:
  port: 8085